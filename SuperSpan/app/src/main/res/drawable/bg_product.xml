<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Bianco un po' più scuro per dare risalto -->
    <solid android:color="@color/bg_product_color" />
    <corners android:radius="16dp" />
    <!-- NIENTE bordo -->
    <padding
        android:left="12dp"
        android:top="12dp"
        android:right="12dp"
        android:bottom="12dp" />
</shape>
